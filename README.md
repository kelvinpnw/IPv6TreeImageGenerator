creates a list of IP addresses to ping to generate an image on the IPv6 Christmas Tree's LED wall.


requirements
`pip install pillow`

usage:

`python image2ipv6.py [filename] -X [x offset] -Y [y offset]`
